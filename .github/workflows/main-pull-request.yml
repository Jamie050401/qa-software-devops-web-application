name: Build/Test

on:
  pull_request:
    branches: [ "main" ]

jobs:
  Build-Test:
    uses: ./.github/workflows/build-test.yml
    with:
      github-workspace: ${{ github.workspace }}
    secrets: inherit
