name: Deploy

on:
  push:
    branches: [ "main" ]

jobs:
  Build-Test:
    uses: ./.github/reusable-workflows/build-test.yml
    with:
      github-workspace: ${{ github.workspace }}
    secrets: inherit

  Publish:
    needs: Build-Test
    uses: ./.github/reusable-workflows/publish.yml
    secrets: inherit

  #Deploy:
  #  needs: Publish
  #  uses: ./.github/reusable-workflows/deploy.yml
  #  secrets: inherit
